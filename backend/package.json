{
  "name": "online-compiler-backend",
  "version": "1.0.0",
  "description": "Secure multi-language online compiler backend (Python, Java, C++) with Docker sandboxing, JWT auth, and execution history.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node src/seed/seedProblems.js",
    "check": "node --check src/server.js && node --check src/controllers/analyticsController.js && node --check src/services/dockerExecutor.js && node --check src/services/localStore.js"
  },
  "keywords": ["compiler", "online-judge", "docker", "sandbox", "express", "mongodb"],
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
